name: Setup JavaScript
description: Configure Node.js environment with pnpm and dependencies

inputs:
  node-version:
    description: Node.js version to install
    required: false
    default: lts/*
  install-deps:
    description: Whether to install dependencies
    required: false
    default: 'true'
  frozen-lockfile:
    description: Use frozen lockfile for installation
    required: false
    default: 'true'
  fetch-depth:
    description: Number of commits to fetch (0 for full history)
    required: false
    default: '1'

runs:
  using: composite
  steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: ${{ inputs.fetch-depth }}

    - name: Setup pnpm
      uses: pnpm/action-setup@v4

    - name: Setup Node.js ${{ inputs.node-version }}
      uses: actions/setup-node@v6
      with:
        node-version: ${{ inputs.node-version }}
        cache: pnpm

    - name: Install dependencies
      if: inputs.install-deps == 'true'
      shell: bash
      run: pnpm install ${{ inputs.frozen-lockfile == 'true' && '--frozen-lockfile' || '' }}
